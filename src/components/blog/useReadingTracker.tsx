
export const useReadingTracker = (postId?: string, user?: any) => {
  // This is a placeholder hook since authentication was removed
  return {
    isRead: false,
    markAsRead: () => {},
    error: null
  };
};
